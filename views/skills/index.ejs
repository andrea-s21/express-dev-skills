<%- include('../partials/header') %>

<h1>Skills</h1>
<ul>
  <% skills.forEach(function(s) { %>
    <li>
      <a href="/skills/<%= s.skill %>">
        <%= s.skill %>
      </a>
        - 
      <%= s.learned ? 'learned' : 'not learned' %>
    </li>
  <% }); %>
</ul>
<%- include('../partials/footer') %>